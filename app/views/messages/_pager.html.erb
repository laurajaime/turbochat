<div id="pager" class="text-center">
  <% if pagy.next %>
    <%= link_to("Loading more messages...",
        url_for(page: pagy.next),
        data: {
            turbo_frame: "page_handler",
            controller: "autoclick"
        }) %>
  <% end %>
</div>
